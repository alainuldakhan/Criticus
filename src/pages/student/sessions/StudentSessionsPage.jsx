import SessionList from '../../../features/student/sessions/SessionList';

const StudentSessionsPage = () => {
  return (
    <section className="page">
      <SessionList />
    </section>
  );
};

export default StudentSessionsPage;
